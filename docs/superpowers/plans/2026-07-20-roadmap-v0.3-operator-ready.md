# ROADMAP v0.3 "Operator-ready" Edits Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Land the roadmap-checkpoint decisions of 2026-07-20 (spec: `docs/superpowers/specs/2026-07-20-roadmap-v0.3-operator-ready-design.md`, same branch) as one PR editing only `ROADMAP.md`.

**Architecture:** Pure documentation change. Each task is a set of exact old→new text replacements in `ROADMAP.md`, verified by grep, committed separately. The branch-staged spec and this plan are `git rm`'d in the final task so the PR diff against main is `ROADMAP.md` only (branch history preserves both).

**Tech Stack:** Markdown, git, `gh` CLI. Use the Edit tool (or careful manual editing) with the exact strings below.

## Global Constraints

- Only `ROADMAP.md` reaches main. The spec and plan files under `docs/superpowers/` are branch-staged (gitignored; were force-added by exact path) and MUST be removed in the final task before the PR merges.
- No personal/portfolio/job-search framing anywhere — this repo is public.
- Do not touch: win conditions' substance, Track A items A1–A7 content, the honesty mechanism, testing-strategy structure, risks 1–7 (risk 4's harness names change, nothing else).
- Evidence URLs must be copied verbatim from this plan (they were verified live on 2026-07-20).
- Branch: `roadmap/v0.3-operator-ready` (already created, already holds the spec commits).
- Working style: one commit per task, message prefix `docs(roadmap):`. Every commit message in this plan additionally ends with the repo's standard trailer (blank line, then `Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>`), omitted from the command blocks below for brevity.

---

### Task 1: Non-goals and acceptance-harness identity (Pi → omp, OAuth boundary)

**Files:**
- Modify: `ROADMAP.md` (North star ¶, win condition 3, first two Non-goals bullets)

**Interfaces:**
- Produces: the name "Oh-My-Pi (omp)" and the OAuth scope-boundary language that Tasks 2–4 reference. Use "omp" as shorthand after first introduction.

- [ ] **Step 1: Update the north-star harness list**

In `ROADMAP.md`, replace:

```text
you point OpenCode or Pi at Loxo with an OpenRouter key
```

with:

```text
you point OpenCode, Oh-My-Pi, or Claude Code at Loxo with an OpenRouter key
```

- [ ] **Step 2: Update win condition 3's harness pair**

Replace:

```text
a real working session in OpenCode and in Pi, on `loxo/*` tiers
```

with:

```text
a real working session in OpenCode and in Oh-My-Pi (omp), on `loxo/*` tiers
```

- [ ] **Step 3: Replace the "Claude Code as a client" non-goal with the OAuth boundary**

Replace this bullet:

```text
- Claude Code as a client (no Anthropic `/v1/messages` endpoint) — reassessed at the harness checkpoint (v0.3).
```

with:

```text
- Serving consumer-OAuth credentials. Claude Code *does* enter scope as a client via an Anthropic `/v1/messages` endpoint (B5 — harness checkpoint #1, decided 2026-07-20; see Milestones), but strictly for API-key-pattern clients: Anthropic blocks consumer OAuth tokens (Pro/Max subscriptions) outside its own products, and Loxo will never implement workarounds. That population is already outside the cost win's audience (win condition 2), so the terms boundary and the positioning boundary are the same line.
```

- [ ] **Step 4: Update the acceptance-harness non-goal**

In the second non-goals bullet, replace:

```text
OpenCode + Pi are the acceptance tests
```

with:

```text
OpenCode + Oh-My-Pi are the acceptance tests
```

- [ ] **Step 5: Verify**

Run: `grep -n "OpenCode or Pi\|in Pi,\|+ Pi are" ROADMAP.md`
Expected: no output.

Run: `grep -c "Oh-My-Pi" ROADMAP.md`
Expected: `3` (north star, win condition 3, non-goals).

- [ ] **Step 6: Commit**

```bash
git add ROADMAP.md
git commit -m "docs(roadmap): OAuth scope boundary in non-goals; acceptance harness Pi -> omp"
```

---

### Task 2: Track B gains B5; forward-looking harness references

**Files:**
- Modify: `ROADMAP.md` (Track B list end, B2 verification sentence, testing-strategy golden-fixtures bullet)

**Interfaces:**
- Consumes: "Oh-My-Pi (omp)" naming and the non-goals OAuth bullet from Task 1 (B5 links to it).
- Produces: the item id **B5** that Task 3's milestone text references.

- [ ] **Step 1: Append B5 after the B4 bullet**

Insert directly after the `- **B4. Parity scorecard.**` bullet (as a new bullet at the same level):

```text
- **B5. Anthropic `/v1/messages` serving endpoint.** Loxo speaks the Anthropic Messages dialect to Claude Code pointed at it via `ANTHROPIC_BASE_URL`; inbound requests translate to the internal OpenAI-compatible form and reuse the existing routing and forwarding unchanged. The hard parts are streaming SSE event translation and tool-call mapping, so B5 gets its own spec plus golden fixtures captured from real Claude Code sessions *before* build — the same harness-as-spec discipline as the other fixtures. API-key-pattern clients only (see non-goals).
```

- [ ] **Step 2: Update B2's verification sentence**

Replace:

```text
Verified against what OpenCode and Pi actually render.
```

with:

```text
Verified against what OpenCode and omp actually render.
```

- [ ] **Step 3: Update the golden-fixtures harness list**

In the testing-strategy "Golden harness fixtures" bullet, replace:

```text
Capture sanitized real request bodies from OpenCode and Pi sessions
```

with:

```text
Capture sanitized real request bodies from OpenCode and omp sessions (and, once B5 lands, Claude Code); earlier Pi fixtures are retained as informal regression fixtures with no acceptance-run obligation
```

- [ ] **Step 4: Verify**

Run: `grep -n "B5\." ROADMAP.md`
Expected: exactly one Track B definition line (plus the non-goals mention from Task 1 matching `B5 —`).

Run: `grep -n "OpenCode and Pi" ROADMAP.md`
Expected: no output.

- [ ] **Step 5: Commit**

```bash
git add ROADMAP.md
git commit -m "docs(roadmap): add B5 /v1/messages serving endpoint; fixture harnesses to omp"
```

---

### Task 3: Rewrite the Milestones section

**Files:**
- Modify: `ROADMAP.md` (entire `## Milestones` section, from its heading to just before `## Implementation notes`)

**Interfaces:**
- Consumes: **B5** (Task 2), OAuth boundary + omp naming (Task 1).
- Produces: milestone names/numbers (`v0.3 — "Operator-ready."`, `v0.4 — "Visible dial."`, `v0.5 — "Earned share."`) that Task 4's cross-references must match, and the recorded checkpoint #1 decision.

- [ ] **Step 1: Replace the whole Milestones section body**

Replace everything between `## Milestones` and `## Implementation notes` with:

```markdown
- **v0.2 — "Honest cloud."** Feature scope fully merged (A1+A2 observing, B1 cache injection, B3 metadata honesty — plus B2 reasoning, pulled forward from the old v0.3). Remaining to tag: parity scorecard v1 (`docs/parity.md`), golden harness fixtures, and the acceptance check — a real OpenCode session on `loxo/*` populates the adequacy ledger with classified traffic, and shows cache hits in `/v1/spend` and correct context windows. No new features enter v0.2.
- **v0.3 — "Operator-ready."** The recruitment release: everything a potential test operator needs to get the point quickly, see a visible payoff, and start contributing evidence. Contents: the OpenTelemetry trace emitter (observe-only; spec: `docs/specs/otel-trace-emitter.md`), B5 `/v1/messages` translation layer, central ledger collector v1 deployed with opt-in ledger push (invariants in the implementation notes below; transport decided in the collector's own spec), a README/pitch rework leading with visible-payoff artifacts (trace-waterfall screenshot, real `/v1/spend` excerpt, scorecard link), onboarding polish, and omp's first acceptance run.

  **Harness checkpoint #1 — decided 2026-07-20** (taken early at a roadmap checkpoint; it was scheduled for this release because "if the answer is yes, later is more expensive than sooner"):
  - *Claude Code enters scope* (B5). Terms: Anthropic blocks consumer OAuth tokens outside its own products, but the genuine CLI pointed at a custom `ANTHROPIC_BASE_URL` with API-key-style auth is supported, and neither the Commercial Terms nor the AUP prohibit multi-model routing ([analysis](https://autonomee.ai/blog/claude-code-terms-of-service-explained/), [anthropics/claude-code#5577](https://github.com/anthropics/claude-code/issues/5577), [y-router precedent](https://github.com/luohy15/y-router)). Demand: Claude Code remains a top harness by token volume even on open-weight models ([Kimi K3](https://openrouter.ai/moonshotai/kimi-k3), ~226B tokens/30d as of 2026-07-18). The excluded OAuth population is already outside the cost win's audience.
  - *The Pi acceptance slot passes to [Oh-My-Pi](https://github.com/can1357/oh-my-pi)* — omp's richer request shapes (subagents, larger tool inventories) stress the classifier the way real traffic will, while Pi stays deliberately minimal; the acceptance set stays bounded at two harnesses. Pi fixtures are retained informally.

  Acceptance — a stranger-shaped test, following only the README: (a) an operator gets OpenCode routing through Loxo in minutes; (b) they see a per-request trace waterfall in a local Jaeger, including the local→cloud fallback hop as a child span; (c) a real Claude Code session pointed at Loxo via `ANTHROPIC_BASE_URL` completes a working session on `loxo/*` tiers; (d) the suite is green and omp's acceptance session is run and scored; (e) a push-enabled router delivers ledger increments to the deployed collector (verifiably resend-safe), while a down collector or unset push leaves routing and local ledgers entirely unaffected.
- **v0.4 — "Visible dial."** A4 shadow evaluation on chore classes, A5 `/v1/dial`, A7 bench suite v1 (separate ledger from day one), and the first human-flipped chore-class promotion. Acceptance: measurable local share on at least one class, defended by shadow-derived adequacy stats.
- **v0.5 — "Earned share."** A6 promotion recommendations, scorecard v2.
- **Cadence.** Each subsequent minor release repeats the loop: parity gaps found by real sessions, dial share expanded by evidence, harness set reassessed.
```

- [ ] **Step 2: Verify**

Run: `grep -n "Visible dial\|Operator-ready\|Earned share" ROADMAP.md`
Expected: exactly one heading-bullet each, in order v0.3 Operator-ready, v0.4 Visible dial, v0.5 Earned share.

Run: `grep -ni "harness checkpoint" ROADMAP.md`
Expected: matches only in the non-goals bullet (Task 1's text), the decided-2026-07-20 block, and (until Task 4 runs) the open-questions line.

- [ ] **Step 3: Commit**

```bash
git add ROADMAP.md
git commit -m "docs(roadmap): milestones renumbered; checkpoint #1 recorded as decided 2026-07-20"
```

---

### Task 4: Collector retiming, recruitment timing, open-questions cleanup, risk 4 naming

**Files:**
- Modify: `ROADMAP.md` (state-architecture pooling paragraph, risk 8 disposition, risk 4 harness names, Open questions list)

**Interfaces:**
- Consumes: milestone names from Task 3 ("v0.3 — Operator-ready"), collector scope language from the spec's D6.

- [ ] **Step 1: Retime the collector in the state-architecture notes**

Replace:

```text
Implementation waits for the second operator or the first real backup need, whichever comes first; until then, the invariants are the deliverable.
```

with:

```text
Implementation lands in v0.3 ("Operator-ready"): that release exists to produce the second operator, so the deferral trigger fires just-in-time — collector v1 is deployed and the router gains an opt-in push client (unset/`""` = fully off) ahead of recruitment. The invariants above govern the build; transport, deployment target, and operational cost are decided in the collector's own spec.
```

- [ ] **Step 2: Update risk 8's recruitment timing**

Replace:

```text
tracked in issue [#6](https://github.com/brendanlefebvre/loxo-llm-router/issues/6), targeted after v0.2 ships an adequacy ledger worth pooling.
```

with:

```text
tracked in issue [#6](https://github.com/brendanlefebvre/loxo-llm-router/issues/6), activated after v0.3 — the operator-ready release built to be recruitment's landing page.
```

- [ ] **Step 3: Update risk 4's harness names**

Replace:

```text
OpenCode/Pi are model-agnostic.
```

with:

```text
OpenCode/omp are model-agnostic.
```

- [ ] **Step 4: Clean up the Open questions list**

Remove this line entirely:

```text
- Whether `/v1/messages` (Claude Code as client) enters scope at harness checkpoint #1.
```

Replace:

```text
- Central ledger collector transport (HTTP push to a small collector service vs. object storage) — decided when the second operator or first backup need arrives; the append-only invariants above keep both options open.
```

with:

```text
- Central ledger collector transport (HTTP push to a small collector service vs. object storage) — decided in the v0.3 collector spec; the append-only invariants above keep both options open.
```

- [ ] **Step 5: Verify**

Run: `grep -n "second operator or" ROADMAP.md`
Expected: no output (both former occurrences rewritten).

Run: `grep -n "enters scope at harness" ROADMAP.md`
Expected: no output.

- [ ] **Step 6: Commit**

```bash
git add ROADMAP.md
git commit -m "docs(roadmap): collector builds in v0.3; recruitment post-v0.3; open questions pruned"
```

---

### Task 5: Whole-document consistency sweep

**Files:**
- Modify: `ROADMAP.md` (only if the sweep finds stragglers)

- [ ] **Step 1: Sweep for stale harness names**

Run: `grep -n "\bPi\b" ROADMAP.md`
Expected: matches only where Pi is deliberately historical/informal — the checkpoint #1 decision block ("Pi acceptance slot passes"), the "Pi fixtures are retained" clauses, and "Oh-My-Pi". Anything presenting Pi as a *current acceptance harness* is a bug: fix it to omp per Task 1/2 phrasing.

- [ ] **Step 2: Sweep for stale milestone numbers**

Run: `grep -n "v0\.[2-5]" ROADMAP.md`
Expected: v0.2 only as "Honest cloud"/close-out references; v0.3 only as "Operator-ready" (or the collector spec); v0.4 only as "Visible dial"; v0.5 only as "Earned share". The old line "Harness checkpoint #1 pulled forward to v0.3" and any "begins v0.4" translation-layer remnant must be gone (superseded by Task 3's rewrite). Fix any straggler to the new numbering.

- [ ] **Step 3: Render check**

Run: `grep -c "^## " ROADMAP.md`
Expected: same count as on main (`git show main:ROADMAP.md | grep -c "^## "`) — section structure unchanged.

- [ ] **Step 4: Commit (only if fixes were made)**

```bash
git add ROADMAP.md
git commit -m "docs(roadmap): consistency sweep after v0.3 renumbering"
```

---

### Task 6: Strip branch-staged docs, push, open PR

**Files:**
- Delete (from branch tip, preserved in history): `docs/superpowers/specs/2026-07-20-roadmap-v0.3-operator-ready-design.md`, `docs/superpowers/plans/2026-07-20-roadmap-v0.3-operator-ready.md`

- [ ] **Step 1: Remove the staged spec and plan**

```bash
git rm docs/superpowers/specs/2026-07-20-roadmap-v0.3-operator-ready-design.md \
       docs/superpowers/plans/2026-07-20-roadmap-v0.3-operator-ready.md
git commit -m "docs: drop branch-staged spec and plan before merge"
```

- [ ] **Step 2: Verify the PR diff is ROADMAP.md only**

Run: `git diff main --stat`
Expected: exactly one file, `ROADMAP.md`.

- [ ] **Step 3: Push and open the PR**

```bash
git push -u origin roadmap/v0.3-operator-ready
gh pr create --title "roadmap: v0.3 becomes Operator-ready; harness checkpoint #1 decided" --body "$(cat <<'EOF'
Roadmap checkpoint of 2026-07-20 (spec in branch history):

- v0.2 close-out gate stated: scorecard v1 + golden fixtures + acceptance session, then tag.
- v0.3 re-scoped as **Operator-ready**: OTel trace emitter, B5 /v1/messages translation layer, central ledger collector v1 + opt-in push, pitch/README rework, onboarding polish, omp first acceptance run. Dial machinery moves to v0.4 (Visible dial); Earned share becomes v0.5.
- **Harness checkpoint #1 decided early**: Claude Code enters scope (API-key pattern only, consumer-OAuth workarounds are a standing non-goal); the Pi acceptance slot passes to Oh-My-Pi.
- Central ledger collector retimed: the operator-ready release is the "second operator" trigger arriving just-in-time.

🤖 Generated with [Claude Code](https://claude.com/claude-code)
EOF
)"
```

Expected: PR URL printed; diff shows only `ROADMAP.md`.
