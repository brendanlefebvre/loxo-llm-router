# Secrets — the OpenRouter key lives at ~/.config/llm-router/env, NEVER in the repo
.env
env
*.env
.config/
secrets*

# Python
__pycache__/
*.pyc
*.pyo
.venv/
venv/

# OS
.DS_Store

# Packaging / build
*.egg-info/
build/
dist/

# Tooling / editor / agent state — this repo is public, these never ship
.claude/
.superpowers/
.pytest_cache/
vendor/
docs/superpowers/

# Local config / container env
loxo.toml
loxo.env

# Spike output (raw usage evidence; summarized into docs/spikes/)
spike-results.jsonl
